name: treat 😺

on:
  pull_request_target:
    types:
      - opened
      - reopened

permissions:
  pull-requests: write  # Allows posting comments on PRs
  issues: write

jobs:
  aCatForCreatingThePullRequest:
    name: 'A treat for your effort!'
    runs-on: 'ubuntu-latest'
    steps:
      - uses: parthiv13/treat@alpha
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
