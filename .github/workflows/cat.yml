name: Cats 😺

on:
  pull_request_target:
    types:
      - opened
      - reopened

permissions:
  pull-requests: write  # Allows posting comments on PRs
  issues: write

jobs:
  aCatForCreatingThePullRequest:
    name: 'A cat for your effort!'
    runs-on: 'ubuntu-latest'
    steps:
      - uses: parthiv13/treat@v0.2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
